# 终端操作手册（无 sudo Linux 版）

> 直接复制每个代码块到终端 paste，按回车跑

---

## 🔧 准备工作

### 0. 确认 U 盘路径

打开终端（`Ctrl + Alt + T`），paste 这个看 U 盘名字：

```bash
ls /media/$USER/
```

记住输出的盘名（比如 `DADAGAGA`）。下面所有命令默认用 `DADAGAGA`，**如果你 U 盘不是这名字，把下面所有命令里的 `DADAGAGA` 替换成你的盘名**。

---

## 🚀 主流程（4 步）

### 1️⃣ 把 U 盘内容拷到桌面

```bash
cp -r /media/$USER/DADAGAGA/中关村学院_Claude配置_20260523 ~/Desktop/zgc
cd ~/Desktop/zgc
```

> 这里 `zgc` 是简短英文名，避免之后中文输入问题。

### 2️⃣ 找到并进入有脚本的那一层

```bash
cd 带* 2>/dev/null || cd .
ls
```

应该能看到：`linux一键安装_无sudo.sh`、`configs/`、`skills_全量备份/` 等。

### 3️⃣ 跑一键安装

```bash
bash linux*sudo.sh
```

用 `*` 通配符，不用打中文。脚本会：
- 装 nvm + Node.js（无 sudo，在 ~/.nvm）
- 装 Claude Code CLI
- 配 4 个 channel（aipai/micu/codesuc/swarm）
- 写 4 个 bash 函数到 ~/.bashrc
- 装 34 个 skill（会问你要不要装，输入 Y）

### 4️⃣ 激活配置 + 测试

```bash
source ~/.bashrc
claudeaipai
```

进入 Claude 后试输入：
```
你能看到几个 skill？什么 model？
```
预期：**34 skills，opus[1m]**。

---

## 🆘 排障

### A. 找不到 linux*sudo.sh

```bash
# 全盘搜
find ~/Desktop -name "linux*.sh" 2>/dev/null
find /media -name "linux*.sh" 2>/dev/null
```

把输出截图给杨镜池。

### B. nvm 下载失败（GitHub 不通）

手机开热点，联想本连热点，重新跑 step 3。

### C. npm 慢

脚本里已经设了国内源，但如果还慢：
```bash
npm config set registry https://registry.npmmirror.com
```

### D. `bash linux*.sh` 报 No such file

你不在对的目录里。先：
```bash
pwd
ls
```
确认能看到 `.sh` 文件。看不到就 `cd 带*` 进子目录。

### E. claude 命令找不到

```bash
source ~/.bashrc
which claude
```

应输出类似 `/home/user/.nvm/versions/node/v20.../bin/claude`。

如果 `which claude` 没输出：
```bash
# 重新激活 nvm
export NVM_DIR="$HOME/.nvm"
. "$NVM_DIR/nvm.sh"
which claude
```

### F. 4 个 channel 全连不上

```bash
# 测连接
curl -I https://api.aipaibox.com/
curl -I https://www.micuapi.ai/
curl -I https://main-new.codesuc.top/
curl -I https://byteswarm.ai/claude
```

哪个能通就用哪个。全不通是校园网代理问题，问机房。

---

## 📋 4 个 Channel 命令

setup 完后这 4 个命令任选其一：

```bash
claudeaipai      # 主用
claudemicu       # 备用 1
claudecodesuc    # 备用 2
claudeswarm      # 备用 3
```

任一进入后用 `/resume` 可以调出历史会话（4 channel 共享 ~/.claude）。

---

## 🔥 一键命令（如果上面都失败，最后兜底）

如果上面那个脚本怎么都跑不起来，**直接在终端 paste 下面这一大段**（自包含、不依赖 U 盘脚本）：

```bash
# === 兜底全自动安装 ===
set -e

# 找到 U 盘上的 profiles.json
PROFILES=$(find /media -name "profiles.json" 2>/dev/null | head -1)
if [ -z "$PROFILES" ]; then
    echo "❌ 找不到 profiles.json，确认 U 盘挂着"
    exit 1
fi
echo "用配置文件: $PROFILES"

# 1. nvm + Node
if ! command -v node >/dev/null; then
    curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
    export NVM_DIR="$HOME/.nvm"
    . "$NVM_DIR/nvm.sh"
    export NVM_NODEJS_ORG_MIRROR="https://npmmirror.com/mirrors/node/"
    nvm install --lts
fi

# 2. Claude Code
if ! command -v claude >/dev/null; then
    npm config set registry https://registry.npmmirror.com
    npm install -g @anthropic-ai/claude-code
fi

# 3. 4 个 channel
python3 - "$PROFILES" <<'PY'
import json, os, sys
HOME = os.path.expanduser('~')
data = json.load(open(sys.argv[1]))
for p in data['profiles']:
    suffix = p['command'].replace('claude','',1)
    d = f"{HOME}/.claude_{suffix}"
    os.makedirs(d, exist_ok=True)
    s = {"env":{"ANTHROPIC_BASE_URL":p['base_url'],"ANTHROPIC_AUTH_TOKEN":p['api_key']},
         "model":p.get('model','opus[1m]'),
         "permissions":{"defaultMode":"bypassPermissions"},
         "effortLevel":"high","includeCoAuthoredBy":False,"skipDangerousModePermissionPrompt":True}
    json.dump(s, open(f"{d}/settings.json",'w'), indent=2)
    print(f"  ✅ {p['command']}")
PY

# 4. bash 函数
grep -q "Claude channels zgc" ~/.bashrc || cat >> ~/.bashrc <<'BRC'

# === Claude channels zgc ===
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"
_claude_p() { local d=$1 n=$2; shift 2; unset ANTHROPIC_AUTH_TOKEN ANTHROPIC_API_KEY ANTHROPIC_BASE_URL ANTHROPIC_MODEL; export CLAUDE_CONFIG_DIR=$d CLAUDE_CHANNEL_NAME=$n; command claude --setting-sources=user "$@"; }
claudeaipai()   { _claude_p "$HOME/.claude_aipai"   "aipai"   "$@"; }
claudemicu()    { _claude_p "$HOME/.claude_micu"    "micu"    "$@"; }
claudecodesuc() { _claude_p "$HOME/.claude_codesuc" "codesuc" "$@"; }
claudeswarm()   { _claude_p "$HOME/.claude_swarm"   "swarm"   "$@"; }
BRC

# 5. skill（如果想跳过删掉这一段）
SKILLS_DIR=$(find /media -name "skills_全量备份" -type d 2>/dev/null | head -1)
if [ -n "$SKILLS_DIR" ]; then
    mkdir -p ~/.config/agents/skills ~/.claude/skills
    cnt=0
    for s in "$SKILLS_DIR"/*/; do
        n=$(basename "$s")
        rm -rf "$HOME/.config/agents/skills/$n" "$HOME/.claude/skills/$n"
        cp -r "$s" "$HOME/.config/agents/skills/$n"
        ln -sf "$HOME/.config/agents/skills/$n" "$HOME/.claude/skills/$n"
        cnt=$((cnt+1))
    done
    echo "✅ $cnt 个 skill"
fi

# 6. CLAUDE.md
CLAUDE_MD=$(find /media -name "CLAUDE.md" -path "*configs*" 2>/dev/null | head -1)
[ -n "$CLAUDE_MD" ] && mkdir -p ~/.claude && cp "$CLAUDE_MD" ~/.claude/ && echo "✅ CLAUDE.md"

echo ""
echo "============================================"
echo "  ✅ 全部完成"
echo "============================================"
echo "现在跑："
echo "  source ~/.bashrc"
echo "  claudeaipai"
```

---

## 📞 实在不行

把以下命令的输出**完整截图**发给杨镜池：

```bash
pwd
ls -la
which node claude
ls /media/$USER/
find ~/Desktop -name "*.sh" 2>/dev/null | head -10
```

---

*最后修订：2026-05-23*
